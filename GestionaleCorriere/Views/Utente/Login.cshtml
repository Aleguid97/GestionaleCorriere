@model GestionaleCorriere.Models.Utenti

@{
    ViewBag.Title = "Login";
}

<style>
    .card {
        margin-block: 10%;
        margin-inline: 10%;
    }

    .card-body {
        text-align: center;
    }

    .form-horizontal {
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    .control-label {
        text-align: left;
    }
</style>

<div class="container">
    <div class="row">
        <div class="col-md-4 mt-4">
            <h2 class="mt-5">Benvenuti in Caronte Trasporti</h2>
            <p>
                Caronte Trasporti è la scelta ideale per soddisfare le tue esigenze di trasporto in modo affidabile e efficiente.
                Con anni di esperienza nel settore, ci impegniamo a fornire servizi di trasporto di alta qualità, garantendo la sicurezza e la puntualità delle tue spedizioni.
                La nostra missione è semplificare il processo di logistica per te, offrendo soluzioni personalizzate che si adattano alle tue specifiche richieste commerciali.
            </p>
        </div>
        <div class="col-md-4 mt-5">
            <div class="card shadow">
                <div class="card-body">
                    <h2 class="card-title">Accedi</h2>

                    @using (Html.BeginForm(null, null, FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
                    {
                        @Html.AntiForgeryToken()

                        <div class="form-group">
                            @Html.LabelFor(model => model.Username, htmlAttributes: new { @class = "control-label mt-3" })

                            @Html.EditorFor(model => model.Username, new { htmlAttributes = new { @class = "form-control" } })
                            @Html.ValidationMessageFor(model => model.Username, "", new { @class = "text-danger" })
                        </div>

                        <div class="form-group">
                            @Html.LabelFor(model => model.Password, htmlAttributes: new { @class = "control-label mt-3" })

                            @Html.EditorFor(model => model.Password, new { htmlAttributes = new { @class = "form-control" } })
                            @Html.ValidationMessageFor(model => model.Password, "", new { @class = "text-danger" })
                        </div>

                        <div class="form-group">
                            <button type="submit" class="btn btn-outline-secondary btn-sm mt-4">Login</button>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
